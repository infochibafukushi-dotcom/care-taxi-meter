# Auth V2 runtime flags for Cloud Functions (care-taxi-meter)
# Keep both false until an explicit production cutover approval.
AUTH_V2_ENABLED=false
AUTH_V2_ENFORCE=false
AUTH_MAX_LOGIN_FAILURES=5
AUTH_LOGIN_LOCK_MINUTES=15
